# vibe-coding

## 하네스: knowledge-wiki 기술 글 파이프라인

**목표:** IT 전분야 기술 글을 작성(에반젤리스트+CTO 시각)→검증(최신성·팩트·오탈자·윤문)→발행(OKF 위키)까지 자동 처리.

**트리거:** 기술 글 작성+발행이 함께 요청되면 `wiki-post` 스킬을 사용하라 (예: "~에 대해 글 써서 위키에 올려줘"). 완성된 노트를 올리기만 할 땐 `wiki-note` 스킬. "가독성/최종 검수만 다시"는 `wiki-post` Phase 3.5(readability-reviewer)로 부분 재실행. 단순 질문은 직접 응답 가능.

**종속 문서:** 하네스(에이전트·스킬·게이트) 변경 시 위키 `content/위키-하네스.md`와 레포 README의 하네스·비용 섹션을 같은 커밋에서 갱신하라.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-05 | 초기 구성 (tech-writer·fact-checker·copy-editor + tech-writing·wiki-verify·wiki-post) | 전체 | - |
| 2026-07-05 | copy-editor에 '대기 금지' 지침 추가 | agents/copy-editor.md | 첫 실행에서 humanize-korean 대기 중 정지 → SendMessage 재개 필요했음 |
| 2026-07-05 | 병합 게이트(validate-note.py)·수치 전수/UNVERIFIED 발행 규칙·경계선 재확인 추가 | skills/wiki-post, wiki-verify | [[에이전트-평가-evals]] 원칙(MAST 검증실패·오류전파·pass@1)을 하네스 자신에 적용 |
| 2026-07-05 | 마크다운 렌더 검증·근사표기 규칙 + 종속 문서 동기화 규칙 | wiki-verify·validate-note.py·위키-하네스·README | 발행물에서 리터럴 `**`·`~` 노출 (규칙 부재) |
| 2026-07-12 | §2에 검증 규칙 2개 추가: ⑦ 상류 근거 파일도 검증 대상(오케스트레이터 사실 추출물·리서치 산출물은 1차 소스 아님), ⑧ 해석 검증(수치가 맞아도 그 위의 인과·비교·일반화를 별도 반증) | wiki-verify·위키-하네스·README | 오케스트레이터가 원문에서 뽑은 사실 파일의 오류가 초안까지 전파됐고, 잡힌 건 규칙이 아니라 프롬프트에 우연히 적어준 덕이었음. 또 SkillOpt·하네스의-미래 두 글의 FAIL이 모두 틀린 수치가 아니라 맞는 수치 위의 틀린 해석에서 남 |
| 2026-07-17 | 최종 가독성 게이트 신설: readability-reviewer 에이전트 + readability-review 스킬 추가, wiki-post에 Phase 3.5(병합 뒤·발행 앞) 배선. 위키-하네스·README 동기화 | agents/readability-reviewer·skills/readability-review·wiki-post·위키-하네스·README | 윤문(copy-editor)·병합을 통과하고도 발행본에 AI 티·난해 용어·비자연 문장이 남았음. 병렬 윤문은 라인 단위라 통독 인상을 못 봄 — 발행본을 처음부터 끝까지 독자 눈으로 판정하는 검수자가 파이프라인에 없었다 |
| 2026-07-17 | tech-writing에 '연결의 근거' 규칙 추가 — 두 사실을 잇는 문장(인과·비교·일반화)은 그 **연결**의 근거를 대야 하고, 척도가 다른 수치는 비교 금지. §5 자기 점검에 대응 항목 1줄 | skills/tech-writing·위키-하네스·README | 해석 FAIL 3연속(SkillOpt 인과 / 하네스의-미래 일반화 / AIDE² 척도 비교)의 공통 구조가 **엣지**였음 — 노드(사실 하나하나)는 통과하는데(AIDE² 수치 22/22 일치) 사실 둘을 잇는 자리에서 무너진다. 기존 규칙("모든 주장에 근거")이 안 문 이유: 양 끝 수치엔 근거가 있었고 없던 건 *둘을 이어도 된다는 근거*였다. 검증엔 ⑧이 있는데 작성엔 대응물이 없어 매번 만들고 매번 잡았음(회당 약 126k = 사이클의 약 27%). 탐지기 추가 대신 **⑧을 작성 쪽에 미러링**. 근거는 실측 — AIDE²에서 척도 비교를 콕 집어 경고한 곳은 지켰고 경고 안 한 곳에서 넘어졌으니 능력이 아니라 커버리지 문제. 과교정(전부 헤지 → 글이 죽음)이 FAIL보다 나쁘므로 "두 사실이 만나는 자리"만 좁게 겨냥. **다음 2~3편에서 해석 FAIL이 안 줄면 규칙이 아니라 구조 문제** |
| 2026-07-17 | wiki-post Phase 1에 '선행 리서치' 단계 코드화 + 리서치 프롬프트 필수 2조항: ⑴ **엣지 금지**(리서치는 사실만 수집, 인과·비교·일반화는 만들지 않음, 출처 다른 수치끼리 비교 금지), ⑵ **산출 파일 맨 위 자기 경고 헤더** 의무 | skills/wiki-post·위키-하네스·README | ⑦은 *검증* 규칙이라 하류가 근거 파일을 **어떻게 대할지**는 규정하지 않았음. AIDE² 편이 우연히 대조군 — 경고를 박은 `00_critique_leads.md`는 작가가 지켰고, 같은 경고가 없던 `00_research.md`의 척도 비교는 framing째로 물려받아 초안까지 감(그게 그 편의 FAIL). 리서치 단계가 스킬에 아예 없어 매번 즉흥 프롬프트였던 것도 원인. 규칙을 오케스트레이터 기억에 맡기지 말고 **파일이 경고를 들고 다니게** 한다 |
| 2026-07-17 | tech-writing에 '집계 방법 병기' 규칙 추가 — 직접 센 수치(개수·바이트·빈도)엔 재현 명령·집계 범위를 함께 남긴다. §5 자기 점검 1줄. 1차 출처가 발표한 수치는 제외(출처 링크가 곧 방법) | skills/tech-writing·위키-하네스·README | 전 위키 최신성 스윕(34편 스캔 → 위험군 10편) 실측. **논지는 안 썩는데 직접 센 수치는 전부 썩었다** — kinetics 편은 사이트가 99→117개로 개편됐는데도 논지 8개가 전부 재현됨. 문제는 다시 세는 게 아니라 *같은 자로* 다시 세는 것이었고, 집계 범위를 본문에 적어둔 수치만 재집계에 성공했다. 방법을 안 적은 수치는 세는 사람마다 갈려(`ease` 82 vs 89 vs 101) 완화 표기로 후퇴 |
| 2026-07-17 | **하네스 무변경 · 실측만 갱신.** README 비용표에 AIDE² 행 추가(소계 327k + 수정루프 126k ≈ 475k)·단계별 범위 상향·요약 375–425k → 375–475k, 위키-하네스 실전 기록에 AIDE² 편 추가 | 위키-하네스·README | 가독성 게이트 포함 첫 사이클이라 README가 스스로 달아둔 단서("게이트 도입 전 수치")를 해소함. 규칙 ⑦이 **설계대로** 작동한 첫 사례 — fact-checker가 리서치 산출물을 되짚어 FAIL 출처를 00_research.md로 특정(07-12엔 프롬프트에 우연히 적어준 덕이었음). FAIL이 해석에서 난 건 이로써 3연속이고, 이번엔 수치 22건 전수 대조 불일치가 0건인데도 그랬다 |
| 2026-07-19 | **wiki-debug 스킬 신설** (tinker-cookbook `/tinker:debug` 차용) — 발행 사후 진단(게이트 판정·FAIL 분류·⑦ 상류 전파)과 ⑥ 회귀 계측 원장 행 초안을 얇게 코드화(스크립트 없음, 분류는 판단 필요). wiki-post Phase 5의 원장 기록 단계가 이 스킬을 호출 | skills/wiki-debug·skills/wiki-post·위키-하네스·README | tinker-cookbook 발행 실측. 원장 행을 **낡은 상태에 대고 써서** push 충돌 후 재작성했다 — 그새 다른 세션이 turbovec 편으로 ⑥을 확정했는데 확정 전 상태로 행을 씀. 병목은 진단·형식화가 아니라 "**행 쓰기 전 현재 원격 원장을 재대조**"라는 절차였고 그게 wiki-debug 3단계의 존재 이유. tinker가 실행(run)과 진단(debug)을 별 스킬로 가른 걸 그대로 차용 — 우리도 wiki-post(실행)/wiki-debug(진단) 분리 |
| 2026-07-19 | **wiki-debug 후속 — 스킬 미러 동기화 + README 스킬 로스터.** ⑴ 위키 레포의 하네스 미러(`claude-skill/skills/`·`.claude/skills/`)에 wiki-debug 본체를 추가하고 낡은 wiki-post 사본을 최신화. ⑵ README 경로 2에 파이프라인 6개 스킬 로스터 표 추가(전체 구조는 위키-하네스 링크) | claude-skill/·.claude/·README | 처음 wiki-debug 커밋은 **문서(위키-하네스·README)만 올리고 스킬 본체를 안 올렸다** — 스킬 런타임은 `~/.claude/skills/`인데 레포가 그걸 두 곳에 미러링한다는 걸 놓쳤다. 확인해보니 미러의 wiki-post는 turbovec 세션의 ⑥ 확정 업데이트조차 빠진 채 낡아 있었다 = **런타임↔레포 미러 드리프트가 이미 누적**. 교훈: 스킬을 바꾸면 런타임 편집으로 끝내지 말고 레포 미러 두 곳도 같은 사이클에 동기화(발행물처럼 미러도 종속 문서). 로스터는 사용자 요청 — README에 스킬을 낱개로 보여주는 표가 없어 wiki-debug만이 아니라 전체가 안 보였다 |
| 2026-07-19 | **규칙 ⑥ 측정 종료 → 확정: 유효.** wiki-post Phase 5의 "⑥ 측정 중" 절을 "원장 기록(회귀 계측)" 절로 교체(3편 판정 machinery 제거, FAIL 분류·원장 기록 습관은 유지). 위키-하네스에 창 상태 6편·turbovec 편 기록·⑥ 확정 판정 추가 | skills/wiki-post·위키-하네스·README | ⑥ 도입(07-17) 후 6편 창에서 해석 FAIL은 1편(서베이, 규칙 도입 *직전* 편)뿐이고, §99·§101이 확정 조건으로 걸어둔 **"비토론형·해석 고밀도 단일 tech-writer 편"이 turbovec-벡터압축으로 도착**했다. 앞선 FAIL 0 편들(핸드북·화면SDLC·그래프·cerebras)은 토론 필터가 리스크를 한 번 걸러 "⑥ 단독 효과"와 섞였는데, turbovec는 토론 없이 ⑧/⑥이 홀로 시험받았고 함정도 진짜였다 — 2차 블로그의 **개체 혼동**(Google=알고리즘 vs 커뮤니티=라이브러리)이라는 새 유형까지 있었으나 초안이 그걸 뼈대로 세우고, 척도 다른 FAISS 비교를 조건별로 쪼개고, "학습 없음→우위" 인과를 좁혔다. 남는 한계: 도입 후 5편 전부 FAIL 0이라 "규칙이 줄였나 vs 원래 안 났나"를 완전 분리 못 하지만, turbovec는 "실패할 함정이 실재했는데 안 났다"를 보여 커버리지 가설([[에이전트-평가-evals]] 노드-엣지)에 부합. 회귀 계측은 계속 |
| 2026-08-29 | **런타임↔미러 포크 화해 + OKF v0.2 실적용 + 규칙 ⑨(압축 위치 검증) 신설.** ⑴ 스킬 5(wiki-verify·tech-writing·wiki-post·readability-review·wiki-note)·에이전트 4를 런타임 기준으로 화해, 스크립트 3종(preflight·validate-domain-brief·validate-note 확장판) 설치. ⑵ 발행 노트 51편을 OKF v0.2로 이관 — 출처 242건 `sources[]`, 각주 205건. ⑶ `check-sync.sh`가 런타임까지 비교하도록 확장. ⑷ wiki-verify §2에 ⑨ 추가, §2.5 도메인·용어 검증 신설 | 스킬 5·에이전트 4·스크립트 3·노트 51편·check-sync.sh·위키-하네스·README | **근본 원인은 규칙 부재가 아니라 규칙이 런타임에 도달하지 못한 것이었다.** 2026-08-03 커밋 둘(cb20f0f·24f61f3)이 위키 레포 미러에만 들어가고 `install.sh`가 안 돌아 26일간 갈라졌다. 그 커밋에 이미 "압축 위치에서 유보 라벨 확인" 조항이 있었는데 효력이 0이었다 — 감시가 런타임→미러 한 방향뿐이라 레포가 앞선 경우를 볼 장치가 없었다. 화해 방향을 런타임으로 잡은 이유: 08-03 재작성이 규칙을 압축하며 ⑥의 과교정 방지 문장("몸을 사리라는 말이 아니다")과 집계 병기 실측 근거를 지웠다 — **규칙 문서 자신에게 일어난 압축 실패**. ⑨는 해석 FAIL 5연속이 전부 요약·결론에서 났고 다섯 편 다 본문에서는 ⑥을 지켰다는 실측에서 나왔다(실패는 지식이 아니라 시야). ⑥ 때처럼 작성 쪽에 미러링하지 않고 **원문을 쥔 검증자 쪽**에 세웠고, 넣자마자 Dogwood 편 발행본의 🔴 1건(`part of` 탈락)을 찾았다 — 세 게이트가 놓친 자리. OKF v0.2는 선언만 되고 적용 0편이었던 것을 실제로 켰다. 각주 41건은 일부러 미부착 — 오귀속이 미부착보다 나쁘고, 표본 8편 44개 전수 대조로 오귀속 0건 확인 |
| 2026-08-30 | **경고 헤더 의무를 상류 산출물 전체로 확대 + 문장 단위 라벨 보존 명시.** wiki-post Phase 1의 자기 경고 헤더·엣지 금지 의무가 `_workspace/00_*.md` **전부**에 걸리도록 범위 확대(서브에이전트 산출물이든 오케스트레이터가 직접 쓴 것이든 동일). 헤더 템플릿에 `[의견]`·`[확인 불가]`·`(추정)` 라벨 보존 한 줄 추가, wiki-verify ⑦에 "라벨이 초안에서 사라졌는지 본다" 명시. **후보로 올려뒀던 신규 규칙 2개(라벨 보존·축소도 연결이다)는 넣지 않음** | skills/wiki-post·skills/wiki-verify·위키-하네스·README | **15편 전수 계측으로 정했다.** ⑦ 상류 오염이 8/15편에 관여했고, 오염이 FAIL까지 간 4편 중 **셋이 오케스트레이터 자신의 파일**에서 발원(`00_source.md` 오전제·오독, `00_debate_*.md` 라벨 소실, `00_session_facts.md` 헤지 뒤집힘). 막힌 편들은 전부 경고 헤더를 단 리서치 산출물. 즉 **검증(⑦)은 오케스트레이터 산출물을 이미 포함하는데 예방만 리서치에 걸린 비대칭**이 구멍이었다 — 새 규칙이 아니라 있던 규칙의 범위 오류이고 근거가 n=3이라 넣었다. 라벨 보존을 별도 규칙으로 세우지 않은 이유: 헤더는 *파일*의 지위를, 라벨은 *문장*의 지위를 말하므로 층이 다른데, ai-prd 실측(헤더 6/6 있었는데 라벨만 떨어져 FAIL)이 보여준 건 새 규칙의 필요가 아니라 헤더 템플릿의 누락이었다. 후보 2개를 뺀 근거도 계측 — 도입 검토 시 n=1이었고 15편이 지나도 **여전히 n=1**(라벨 보존=ai-prd 1편, 축소형=dogwood 1편). 07-17이 못 박은 "과교정이 FAIL보다 나쁘다"를 그대로 적용했다. **규칙은 인상이 아니라 세어서 정한다** |
| 2026-09-06 | **수정 루프 3면 개입 — 발생률·단가·시간.** ⑴ tech-writing §3에 **유보 해소 의무**: `(검증 필요)` 마커를 달기 전에 1차 출처를 1회 열어보고, 못 닫히면 `(1차 확인 시도: …)`를 마커 옆에 남긴다. wiki-verify §2 ①이 그 기록에서 이어받고, 병합에서 제거하며 validate-note.py가 잔존을 차단(테스트 1). ⑵ **렌더 검증을 사람에서 기계로**: `validate-note.py --render-only`(초안 모드) 신설, wiki-post Phase 1 끝에서 초안에 걸어 출력을 copy-editor 입력에 첨부, wiki-verify §3의 렌더 2항을 '게이트 출력에 뜬 자리만 수정'으로 축소(테스트 5). ⑶ **수정 루프 국소 패치 계약**: wiki-post Phase 3.3 + tech-writer 에이전트에 재호출 입력을 FAIL 항목 + 해당 절 + 원문 발췌로 한정, 원문·초안 전문 재적재 금지 | skills/tech-writing·wiki-verify·wiki-post·scripts/validate-note.py(+테스트 6)·agents/tech-writer·위키-하네스·README | **15편 원장 전수 계측.** 루프는 10/15편에서 돌았고 원인이 셋으로 갈렸는데, 상위 둘(압축 위치 n=5·⑦ 상류 n=4)은 08-29·08-30에 이미 손댔고 **남은 미처리가 `유보 → 승격` n=5**였다(turbovec `ICLR 2026`·boris `82%`·claude5 `/doctor`·에이전트-팀·dogwood InfoQ — 다섯 번 다 초안이 안전측으로 격리한 항목을 검증자가 1차를 열어 확정 사실로 승격시켰다). 규칙이 "확신이 낮으면 마커를 남긴다"에서 끝나 **열어보라는 의무가 없던 것**이 구멍이다 — 근거 없는 확장이 오류이듯 근거 없는 유보도 오류이고(dogwood 축소형 FAIL이 같은 이야기의 다른 얼굴), 그 다섯 번은 작가 일을 검증자가 대신한 만큼 팩트체크 시간(3–23분)과 루프로 돌아왔다. 대조군도 원장에 있다: 초안이 검증 전에 스스로 1차까지 내려간 15편은 해석 FAIL 0으로 가장 깨끗했다. ⑵는 **중복 노동 제거** — 게이트가 이미 깨진 강조·물결·펜스를 잡는데 §3이 같은 일을 copy-editor에게 또 시켰고 게이트는 병합 뒤에야 돌아 순서까지 뒤집혀 있었다(copy-editor 실측 1–27분). ⑶은 **루프 단가** — 재검증은 이미 'FAIL 항목만'인데 재작성은 스코프가 없어 재호출 52–90k가 초안 45–95k와 거의 같았다. 비용의 대부분은 출력이 아니라 원문·초안 재적재다(AIDE² 편이 비쌌던 이유도 원문 약 30k의 3회 재적재). **새 규칙·새 에이전트·새 단계는 넣지 않았다** — ⑨ 강화(도입 후 n=1)·집계 방법 게이트화(n=1)·'축소도 연결이다'(n=1)는 08-30 판례대로 보류하고 계측만 계속. 셋 다 계측 전이므로 README 비용표는 갱신하지 않았다 |
